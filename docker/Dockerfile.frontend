# React 개발 서버 사용
FROM node:18-alpine

WORKDIR /app

# package.json과 package-lock.json 복사 후 의존성 설치
COPY package*.json ./
RUN npm install

# 소스코드 복사
COPY . .

# 3000포트 노출
EXPOSE 3000

# React 개발 서버 시작
CMD ["npm", "start"]
